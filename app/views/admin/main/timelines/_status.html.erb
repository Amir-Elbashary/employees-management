<div class="sl-right">
  <div>
    <a href="#" class="link"><%= timeline.owner.full_name %></a> <span class="sl-date"><%= time_ago_in_words(timeline.created_at) %> ago</span>
    <p class="m-t-10"><%= timeline.content.html_safe %></p>
  </div>

  <%= render 'admin/main/timelines/comments' %>
</div>
