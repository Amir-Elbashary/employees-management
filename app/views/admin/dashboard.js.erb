$('.profiletimeline').append('<%= j render 'admin/main/posts', timelines: @timelines %>');

<% if @timelines.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@timelines) %>');
  $('#loading').addClass('d-none');
<% else %>
  $('.pagination').remove();
<% end %>

<% sleep 1 %>
