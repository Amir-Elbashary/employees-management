<%= render 'shared/flash_messages' %>

<%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'form-horizontal form-material', id: 'loginform' }) do |f| %>
  <a href="javascript:void(0)" class="text-center db"><img src="/dashboard/images/logo-icon.png" alt="Home" /><br/><img src="/dashboard/images/logo-text.png" alt="Home" /></a>

  <div class="form-group m-t-40">
    <div class="col-xs-12">
      <%= f.label :email %>
      <%= f.email_field :email, autofocus: true, required: true, autocomplete: "email", class: 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-xs-12">
      <%= f.label :password %>
      <%= f.password_field :password, required: true, class: 'form-control' %>
    </div>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="form-group row">
      <div class="col-md-12">
        <div class="checkbox checkbox-primary pull-left p-t-0">
          <%= f.check_box :remember_me, class: 'checkbox checkbox-primary pull-left p-t-0' %>
          <%= f.label :remember_me %>
        </div>
        <!--
        <a href="javascript:void(0)" id="to-recover" class="text-dark pull-right"><i class="fa fa-lock m-r-5"></i> Forgot pwd?</a>
        -->
      </div>
    </div>
  <% end %>

  <div class="form-group text-center m-t-20">
    <div class="col-xs-12">
      <%= f.submit "Log in", class: 'btn btn-info btn-lg btn-block text-uppercase btn-rounded' %>
    </div>
  </div>
<% end %>

<%#= render "devise/shared/links" %>
